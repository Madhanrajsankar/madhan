# Brew Haven Coffee Shop

A simple static website for a coffee shop.

## Usage

Open `index.html` in your web browser to view the website.

## Features

- Home section with welcome message
- Menu with coffee and pastry items
- About section
- Contact information

## Troubleshooting

If the page doesn't load properly, ensure both `index.html` and `style.css` are in the same directory.